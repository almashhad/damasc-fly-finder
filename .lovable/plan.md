

# تحسين قسم "استكشف أرخص الرحلات" - إضافة عناصر جذابة

## الهدف
جعل القسم أكثر جاذبية وإغراءً بإضافة معلومات حقيقية من قاعدة البيانات تشجع المستخدم على الاستكشاف، مثل أرخص سعر متاح وعدد الوجهات.

## البيانات المتاحة من قاعدة البيانات
- دمشق: أرخص رحلة تبدأ من **$85** (إلى بيروت) - **8 وجهات** متاحة
- حلب: أرخص رحلة تبدأ من **$170** (من الشارقة) - وجهات متعددة

## التحسينات المقترحة

### 1. إضافة "ابتداءً من $XX" على كل بطاقة
- نص صغير أخضر يعرض أرخص سعر حقيقي من قاعدة البيانات
- مثلا: "ابتداءً من $85" بلون أخضر جذاب
- يتم جلبه ديناميكياً باستخدام hook جديد `useMinPriceForAirport`

### 2. إضافة عدد الوجهات
- نص صغير مثل: "8 وجهات متاحة" يظهر تحت اسم المطار
- يعطي إحساس بالتنوع والخيارات

### 3. إضافة شارة "عروض متاحة" صغيرة
- شارة خضراء/ذهبية صغيرة أعلى البطاقة أو بجانب الاسم
- نص مثل: "أسعار مميزة" أو "عروض جديدة"
- بدون مبالغة - فقط لمسة بسيطة

### 4. تحسين العنوان الرئيسي
- تغيير "🗺️ استكشف أرخص الرحلات" إلى عنوان أكثر جاذبية بدون emoji
- مثلا: "أسعار لا تُفوّت" أو "وفّر على رحلتك القادمة"
- إضافة وصف فرعي محفز

### هيكل البطاقة المحسنة

```text
+----------------------------------------------+
| [✈] رحلات دمشق              [أسعار مميزة]  |
|     مطار دمشق الدولي                        |
|     8 وجهات متاحة                    DAM    |
|                                              |
|     ابتداءً من $85              ←            |
+----------------------------------------------+
```

---

## التفاصيل التقنية

### تعديل `src/hooks/useFlights.ts`

**إضافة hook جديد: `useMinPricesForAirports`**
- يجلب أرخص سعر وعدد الوجهات لكل مطار (DAM و ALP)
- يستخدم query واحد لجلب بيانات المطارين معاً
- يجلب من جدول `flights` مع JOIN على `destinations`
- يرجع object بهذا الشكل:
```text
{
  DAM: { minPrice: 85, destinationCount: 8 },
  ALP: { minPrice: 170, destinationCount: 4 }
}
```

### تعديل `src/pages/Index.tsx`

**استيراد الـ hook الجديد:**
- إضافة `useMinPricesForAirports` للاستيرادات

**تعديل قسم الاستكشاف:**
- تغيير عنوان القسم من "🗺️ استكشف أرخص الرحلات" إلى "وفّر على رحلتك القادمة" أو "أسعار لا تُفوّت"
- تغيير الوصف إلى نص أكثر تحفيزاً
- إضافة بيانات `minPrice` و `destinationCount` لكل بطاقة من الـ hook
- إضافة عناصر HTML جديدة داخل `.syria-explore-row`:
  - `.syria-explore-price`: نص "ابتداءً من $85" بلون أخضر
  - `.syria-explore-dest-count`: نص "8 وجهات متاحة"
  - `.syria-explore-tag`: شارة "أسعار مميزة"

### تعديل `src/pages/Index.css`

**كلاسات جديدة:**

| الكلاس | الوصف |
|--------|-------|
| `.syria-explore-price` | نص السعر الأخضر - `font-size: 13px`, `color: hsl(142 71% 35%)`, `font-weight: 700` |
| `.syria-explore-dest-count` | عدد الوجهات - `font-size: 12px`, `color: hsl(var(--muted-foreground))` |
| `.syria-explore-tag` | شارة "أسعار مميزة" - خلفية خضراء فاتحة `hsl(142 60% 93%)`, نص أخضر, `border-radius: 100px`, `font-size: 11px`, `padding: 3px 10px` |

**تعديلات على الكلاسات الموجودة:**
- `.syria-explore-info`: إضافة مساحة للعناصر الجديدة
- `.syria-explore-sub`: تعديل بسيط ليتناسب مع العناصر الإضافية

**تعديل responsive للموبايل:**
- تصغير حجم خط السعر والشارة على الشاشات الصغيرة
- التأكد من عدم تكدس العناصر

### ملخص الملفات المعدلة

| الملف | التغيير |
|-------|---------|
| `src/hooks/useFlights.ts` | إضافة hook `useMinPricesForAirports` لجلب أرخص سعر وعدد الوجهات |
| `src/pages/Index.tsx` | تحديث عنوان القسم + إضافة سعر ابتدائي + عدد وجهات + شارة "أسعار مميزة" |
| `src/pages/Index.css` | إضافة كلاسات للسعر والشارة وعدد الوجهات + تعديل responsive |

